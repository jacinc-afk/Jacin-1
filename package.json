{
  "name": "ringcentral-acculynx-leads",
  "version": "1.0.0",
  "private": true,
  "description": "Turns lead intake posts in RingCentral team chat into leads in AccuLynx.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "discover": "node src/discover.js",
    "test": "node --test 'test/**/*.test.js'",
    "probe:assignment": "node src/probe-assignment.js",
    "map:companies": "node src/company-map.js",
    "sync": "node src/sync.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1"
  }
}
